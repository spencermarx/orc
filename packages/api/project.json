{
  "name": "@orc/api",
  "sourceRoot": "packages/api/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "bun build ./src/index.ts --outdir ./dist --target node --format esm", "cwd": "packages/api" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "packages/api" }
    }
  }
}
