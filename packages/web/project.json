{
  "name": "@orc/web",
  "sourceRoot": "packages/web/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "bun build ./src/index.ts --outdir ./dist --target node --format esm", "cwd": "packages/web" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "packages/web" }
    }
  }
}
